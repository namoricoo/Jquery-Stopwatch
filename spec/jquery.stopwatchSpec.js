describe("Stopwatch", function() {
	var stopwatch;
	beforeEach(function() {
		stopwatch = new JqueryStopwatch();
	});
	
	describe('count up stopwatch', function() {
		it('should have prototype Stopwatch defined', function () {
			expect(Stopwatch).toBeDefined();
		});

	
	});
});